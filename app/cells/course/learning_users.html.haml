- count = @course.selected_users.count
- users = @course.selected_users.limit(20)

.pblock
  - if count > 0
    .cpart.page-course-learning-users
      %h3.cp #{count}人正在学习这个课程
      .users
        - users.each do |user|
          .user
            .avatar= avatar_link user
            .name= user_link user
        .clearfix
  - else
    .cpart.page-course-learning-users.no-user
      %h3.cp 这个课程还没有人学习