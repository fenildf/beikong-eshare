- content_for :header do
  = render_cell :manage_courses_breadcrumb, :design, :model => @course

  .btns
    / %a.btn.gray-flat{:href => "/manage/courses/#{@course.id}/applies"}
    /   %i.icon-list-alt
    /   %span 选课管理
    %a.btn.gray{:href => "/courses/#{@course.id}?preview=true"}
      %i.icon-eye-open
      %span 预览
    %a.btn.success{:href => "/manage/courses/#{@course.id}/chapters/new"}
      %i.icon-plus.icon-white
      %span 增加章节

.page-manage-course-show
  - if @chapters.blank?
    .pblock.desc
      课程 #{@course.name} 下还没有任何章节，点击上方的增加章节按钮来创建一个新的章节

  - else
    .pblock.desc
      你正在对课程 #{@course.name} 进行章节编排，你可以增减章节或调整章节排序

    .pblock.table
      = render_cell :course_ware, :manage_chapter_table, :chapters => @chapters